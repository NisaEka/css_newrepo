import 'package:flutter/material.dart';

class OriginDropdown extends StatelessWidget {
  const OriginDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
